import React from 'react';
import Logo from '../components/Logo';

const Header = () => {
    return (
        <div>
            
            <Logo></Logo>
        </div>
    );
};

export default Header;